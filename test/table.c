int a = 10;
int b = 20;
// dé commentair
--
int ha(int arg0, int arg1) { a = 256; }
int main() {
    a = 55;
    int e = 56;
    int d = 45;
    int e =  = ++ -; // erreur de syntaxe : pas de soucis !
    /* un commentair multi
    laïne ! */
    // utilisation de symboles qui n'existent pas.
    z = y + &x;
}
int hoho(int a, int c)
{
    int* g = &a;
    *g = *g + 5;
    // ha(777, *g, 10 + 20 * 30, (4+(1+(3+(9)))), 100, 200);
    ha(g, &g);
    ha();
    /* samarch 

    */
    ha(1, 2);
    g(56);
    int tt = 5656;
}

